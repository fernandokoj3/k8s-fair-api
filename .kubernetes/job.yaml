# apiVersion: batch/v1
# kind: Job
# metadata:
#   name: migration-job
# spec:
#   template:
#     spec:
#       containers:
#         - name: fair-api-container
#           image: koj3/fair-api:1.0
#           command: ["npm", "run", "migration:run:local"]
#           envFrom:
#             # Obtem do configmap
#             - configMapRef:
#                 name: fair-api-configmap
#           env:
#             - name: APP_DATABASE_PWD
#               valueFrom:
#                 secretKeyRef:
#                   key: POSTGRES_PASSWORD
#                   # Nome do secret
#                   name: database-secret
#       restartPolicy: Never
